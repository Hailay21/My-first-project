Step 1: Install Git & Set Up Authentication
Install Git: Download and install Git on your computer if you haven't already (git-scm.com).

Configure Your Credentials: Open your terminal (Mac/Linux) or Git Bash (Windows) and set your identity:
git config --global user.name "Your Name"
git config --global user.email "your_email@example.com"
